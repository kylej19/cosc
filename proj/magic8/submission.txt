CONSOLE:
THE MAGIC 8BALL

seek and you shall find

what do you seek? why?

It is decidedly so.

do you have any additional queries? (y/N) y

what do you seek? will my code run this time?

Cannot predict now.

do you have any additional queries? (y/N) y

what do you seek? what about now?   

Cannot predict now.

do you have any additional queries? (y/N) n

goodbye.
SOURCE:
#include <iostream>
#include <random>

int main()
{
  // rand
  std::random_device rn_dev; // seed
  std::mt19937  generator(rn_dev()); // engine
  std::uniform_int_distribution<int> distribute(0,7); // implement

  std::string user_query, replay;
  bool flag = true;

  std::string response[8] = { "\nIt is certain.",
    "\nIt is decidedly so.",
    "\nWithout a doubt.",
    "\nCannot predict now.",
    "\nReply hazy, try again.",
    "\nMy sources say no.",
    "\nOutlook not so good.",
    "\nNot a chance."};

  std::cout << "THE MAGIC 8BALL\n\n";
  std::cout << "seek and you shall find";

  while (flag) {

    std::cin.ignore();
    std::cout << "\nwhat do you seek? ";
    getline(std::cin, user_query);

    std::cout << response[distribute(generator)];
    std::cout << "\n\ndo you have any additional queries? (y/N) ";

    //std::cin.ignore();
    std::cin >> replay;

    if(replay == "y" || replay == "Y" || replay == "yes" || replay == "Yes")
      {
	flag = true;
	//std::cin.clear();
      }
    else
      {
	flag = false;
      }
  }

  std::cout << "\ngoodbye.";
  
  std::cout << std::endl;
}
